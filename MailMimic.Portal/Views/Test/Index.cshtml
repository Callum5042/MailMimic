@using MailMimic.ExchangeServer.Models
@model ICollection<MimicMessage>

<h1>Hello from Razor</h1>


@foreach (var message in Model)
{
    <div>
        <h2>@message.Subject</h2>
        <p>@message.Source</p>
    </div>
}